alert(`bienvenido y bienvenida a nuestrapagina WEB`);

let = "luna";

let edad = 25;

let numeVentas = 50;

let saldoDisponoble = 1000;

alert(`¡Error! Completa todos los campos`);

let mensajeDeError = "¡Error! Completa todos los campos";

alert(`${mensajeDeError}`);

let nombre = prompt("ingrese su nombre por favor: ");

let edadU = prompt("ingrese su edad por favor");

if(edadU >= 18){
    alert(`Uds tiiene ${edadU}, y puede obtener su licensia de conducir ya que es mayor de edad`)
}